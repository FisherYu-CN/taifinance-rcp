import React, {Component} from 'react';
import Helmet from 'react-helmet';

export default class PasswordResetInvalid extends Component {

    render() {
        return (
            <div>
                <Helmet title="Password Reset Invalid"/>
                <span>Password Reset Invalid</span>
            </div>
        );
    }
}
